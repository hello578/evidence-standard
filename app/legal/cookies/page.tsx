import type { Metadata } from "next";
import { PageShell } from "../../components/page-shell";
export const metadata: Metadata = { title: "Cookies" };
export default function Cookies(){return <PageShell eyebrow="Legal" title="Cookies" intro="EvidenceStandard.org uses only the cookies or local storage needed for site operation and consent preferences, together with optional measurement where you have agreed to it."><h2>Optional analytics</h2><p>If you allow analytics, Google Analytics 4 may set measurement cookies such as <code>_ga</code> and <code>_ga_*</code> and receive technical and usage information. The Google tag is not loaded before consent. Declining analytics prevents measurement from starting. Your choice is stored locally in this browser and can be cleared through the consent controls or browser site-data settings.</p><p>See <a href="https://policies.google.com/technologies/cookies" target="_blank" rel="noreferrer">Google’s cookie information</a> for more detail.</p></PageShell>}
